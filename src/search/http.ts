import type { SearchConfig } from "./types.js";
export function cors(config: SearchConfig, req: any, res: any): boolean { const origin = String(req.headers?.origin ?? ""); if (req.method === "GET") { res.setHeader("Access-Control-Allow-Origin", "*"); } else if (origin && config.allowedOrigins.includes(origin)) { res.setHeader("Access-Control-Allow-Origin", origin); res.setHeader("Vary", "Origin"); } else { res.setHeader("Access-Control-Allow-Origin", config.allowedOrigins[0] ?? "https://orinai.org"); } res.setHeader("Access-Control-Allow-Methods", "GET, POST, OPTIONS"); res.setHeader("Access-Control-Allow-Headers", "Content-Type, X-Orin-Service-Assertion"); return true; }
export function securityHeaders(res: any): void { res.setHeader("Cache-Control", "no-store"); res.setHeader("Referrer-Policy", "no-referrer"); res.setHeader("X-Content-Type-Options", "nosniff"); res.setHeader("Content-Security-Policy", "default-src 'none'; frame-ancestors 'none'"); }

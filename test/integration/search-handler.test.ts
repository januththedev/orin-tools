import { describe, expect, it } from "vitest";
import { createSearchHandler } from "../../src/search/handler.js";
import { deps, responseRecorder } from "../helpers/fakes.js";
describe("search handler", () => { it("serves anonymous GET and private POST without echo", async () => { const get = responseRecorder(); await createSearchHandler(deps())({ method: "GET", query: { q: "weather in Kandy", n: "2" }, headers: { "x-vercel-forwarded-for": "203.0.113.10" } }, get.res); expect(get.state.status).toBe(200); expect(get.state.body.query).toBe("weather in Kandy"); const post = responseRecorder(); await createSearchHandler(deps())({ method: "POST", body: { query: "private query", n: 2 }, headers: { "x-vercel-forwarded-for": "203.0.113.10", "x-orin-service-assertion": "preview-service" } }, post.res); expect(post.state.status).toBe(200); expect(post.state.body.query).toBeUndefined(); expect(post.state.headers["Cache-Control"]).toBe("no-store"); }); it("rejects private POST without assertion before provider", async () => { const response = responseRecorder(); await createSearchHandler(deps())({ method: "POST", body: { query: "x", n: 1 }, headers: { "x-vercel-forwarded-for": "203.0.113.10" } }, response.res); expect(response.state.status).toBe(401); }); });
